"use client";

// This component is an unused alternate — kept for reference.
// The active request page uses inline form fields matching backend FormRequest.java.

export default function MaterialTable() {
  return (
    <div className="bg-white rounded-xl border border-gray-200 shadow-sm p-6">
      <p className="text-sm text-gray-500">
        Material table component — not currently in use.
      </p>
    </div>
  );
}
